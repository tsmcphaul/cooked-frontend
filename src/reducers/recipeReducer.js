export default function recipeReducer (state, action) {
    
    switch (action.type) {
        case "FETCH_RECIPES":
            return {
                
            }
            
            break;
    
        default:
            break;
    }
    return state;
}